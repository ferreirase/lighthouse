class User {
  constructor ({ id, name, email, phone, password }) {
    this.id = id
    this.name = name
    this.email = email
    this.phone = phone
    this.password = password
  }
}

export default User
